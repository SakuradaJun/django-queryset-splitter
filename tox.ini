[tox]
envlist = py{26,27,32,33,34,35}-django{17,18,19}

[testenv]
deps =
    coverage
    django17: Django < 1.8
    django18: Django < 1.9
    django19: Django < 1.10
commands =
    coverage run py.test
    coverage report
    coverage html
